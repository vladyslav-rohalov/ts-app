import { createTheme } from '@mui/material/styles';
import '@fontsource/poppins';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#090146',
    },
  },
  typography: {
    fontFamily: 'Poppins',
    allVariants: {
      color: '#ffffff',
    },
  },
});
