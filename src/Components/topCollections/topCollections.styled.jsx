import styled from '@emotion/styled';

export const Gallery = styled.ul`
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  gap: 24px;
  list-style: none;
  margin-top: 48px;
  padding: 0;
`;
